<Page x:Class="HN.Bangumi.Uwp.Views.SubjectView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:models="using:HN.Bangumi.Models"
      xmlns:controls="using:HN.Bangumi.Uwp.Controls"
      DataContext="{Binding Source={StaticResource Locator}, Path=Subject}"
      mc:Ignorable="d">
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ScrollViewer>
            <StackPanel Orientation="Vertical">
                <Grid>
                    <Image Source="{x:Bind Path=ViewModel.Subject.Images.Large, Mode=OneWay}"
                           Stretch="None" />
                    <TextBlock VerticalAlignment="Bottom"
                               Text="{x:Bind Path=ViewModel.Subject.ChineseName}"
                               TextWrapping="Wrap" />
                </Grid>
                <TextBlock Text="{x:Bind Path=ViewModel.Subject.Summary, Mode=OneWay}"
                           TextWrapping="Wrap" />
                <GridView ItemsSource="{x:Bind Path=ViewModel.Subject.Characters, Mode=OneWay}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="models:Character">
                            <controls:CharacterControl />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </GridView>
                <TextBlock x:Name="ForTest"
                           TextWrapping="Wrap" />
            </StackPanel>
        </ScrollViewer>
        <Button HorizontalAlignment="Center"
                VerticalAlignment="Bottom"
                Click="ButtonBase_OnClick" />
    </Grid>
</Page>